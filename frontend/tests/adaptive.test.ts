import { describe, it, expect } from 'vitest'
import { computeWeeklySeries } from '../src/lib/analytics'

// This test is a placeholder to ensure test runner config is correct.
describe('placeholder', () => {
  it('runs', () => {
    expect(1+1).toBe(2)
  })
})
